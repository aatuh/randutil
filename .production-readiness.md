## Verdict

`randutil` is release-ready for ordinary production use as of `v2.1.2`.
The concrete blockers from the prior assessment have been remediated and
released:

- `PickByProbability` rejects `NaN` and infinite probabilities with
  `core.ErrInvalidProbability`.
- Full signed integer ranges can return `math.MinInt64` and 64-bit
  `math.MinInt` without a spurious `ErrResultOutOfRange`.
- Deterministic regression coverage was added for the fixed edge cases.
- Distribution non-finite parameter coverage now includes Bernoulli
  probability validation.
- Runtime and audit-tool dependencies were refreshed.
- CI action versions were refreshed, and generated SARIF is normalized before
  upload to avoid duplicate-tag validation failures.
- README, package docs, release docs, changelog, and benchmark evidence were
  updated for the release.

Practical rating after remediation: **around 8/10 production readiness** for
ordinary application use.

## Current Production Boundary

Use `randutil` for ordinary Go application needs such as tokens, random bytes,
UUIDs, ULIDs, NanoIDs, sampling, deterministic tests, and controlled
domain-separated streams.

Do not present it as:

- a FIPS-validated module;
- an audited cryptographic primitive library;
- a replacement for direct `crypto/rand.Reader` use in strict compliance
  contexts;
- a battle-tested dependency for high-risk security SDKs without additional
  review;
- a statistical package with externally validated scientific distributions.

## Release-Readiness Checklist

The `v2.1.2` release-readiness checklist is complete:

1. `make finalize` passed on the release commit.
2. GitHub Go CI, CodeQL, and Scorecard passed for the release commit.
3. Branch protection is enabled for `master` with strict required checks:
   `test`, `analyze`, and `analysis` from GitHub Actions app ID `15368`.
4. No superseded Dependabot PRs remain open.
5. The `v2.1.2` annotated tag was pushed.
6. GitHub release notes were published with the changelog summary, CI links,
   and benchmark summary from `docs/benchmarks-v2.1.2.md`.

## Evidence Captured

- Local targeted tests passed for `collection`, `core`, `numeric`, `dist`,
  `uuid`, and `randstring`.
- `make vuln`, `make gosec`, `make lint`, and `make bench` passed after the
  dependency/tool refresh.
- `v2.1.1` had no benchmark target or benchmark functions, so the
  `v2.1.2` benchmark file records a baseline without claiming performance
  improvement.
- `v2.1.2` was published at
  `https://github.com/aatuh/randutil/releases/tag/v2.1.2`.
